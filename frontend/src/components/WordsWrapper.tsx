import React from 'react'

const WordsWrapper = ({ expected = ["some", "of"] }: { expected: string[] }) => {
    const 
    
    return (<>
        <div className='wordsWrapper'>
            <div className='words'>
                {}
            </div>
        </div>
    </>);
}

export default WordsWrapper;